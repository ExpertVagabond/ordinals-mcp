# project.toml — Machine-readable project manifest for AI agents
# Auto-generated by gen-project-toml.sh — edit freely, won't be overwritten

[project]
name = "ordinals-mcp"
description = "Bitcoin Ordinals MCP Server — inscriptions, runes, BRC-20, collections, marketplace data"
stack = ["rust", "node", "typescript"]
entry = "src/main.rs"
last_commit = "2026-03-31"

[commands]
build = "cargo build"
test = "cargo test"
dev = "cargo run"

[agent]
has_claude_md = false
notes = ""
